/**

**/
(() => {
  'use strict';

  class NotificationManager {
    load(messageCenter) {
      return Promise.resolve();
    }

    unload() {
      return Promise.resolve();
    }
  }

  module.exports = NotificationManager;
})();
